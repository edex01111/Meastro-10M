# Development placeholders — overridden by /run/base44/app.env (real secrets).
# BOT_TOKEN must be a real Telegram bot token; a placeholder here only satisfies
# the "is it set" check but the bot will fail to launch until a real token is supplied.
BOT_TOKEN=dev-placeholder-replace-me
